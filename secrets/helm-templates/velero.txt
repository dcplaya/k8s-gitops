configuration:
  provider: aws
  backupStorageLocation:
    name: aws
    bucket: velero
    use-restic: true
    wait: true
    config:
      region: minio
      s3ForcePathStyle: true
      s3Url: http://minio.velero.svc.cluster.local:9000
      publicUrl: http://minio.cluster.$DOMAIN:9000
  volumeSnapshotLocation:
    name: aws
    resticTimeout: 6h
    config:
      region: minio
credentials:
  secretContents:
    cloud: |
      [default]
      aws_access_key_id = $MINIO_ACCESS_KEY
      aws_secret_access_key = $MINIO_SECRET_KEY
