code-server:
  env:
    PASSWORD: $GENERAL_PASSWORD
  ingress:
    enabled: true
    hosts:
    - host: vscode.devops.$DOMAIN
      paths:
        - /
    tls:
    - hosts:
      - vscode.devops.$DOMAIN
