ingress:
  hosts: [node-red.home.$DOMAIN]
  tls:
  - hosts:
    - node-red.home.$DOMAIN
  annotations:
    nginx.ingress.kubernetes.io/auth-url: "https://auth.$DOMAIN/oauth2/auth"
    nginx.ingress.kubernetes.io/auth-signin: "https://auth.$DOMAIN/oauth2/start"
