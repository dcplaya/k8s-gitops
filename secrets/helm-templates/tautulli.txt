sonarr:
  ingress:
    hosts:
    - host: plexpy.media.$DOMAIN
      paths:
        - /
    - host: tautulli.media.$DOMAIN
      paths:
        - /
    tls:
    - hosts:
      - plexpy.media.$DOMAIN
      - tautulli.media.$DOMAIN
