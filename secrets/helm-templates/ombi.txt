ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: "nginx"
  hosts:
  - requests.media.$DOMAIN
    paths:
      - /
  - request.media.$DOMAIN
    paths:
      - /
  - ombi.media.$DOMAIN
    paths:
      - /
  tls:
  - hosts:
    - requests.media.$DOMAIN
    - request.media.$DOMAIN
    - ombi.media.$DOMAIN
