ingress:
  hosts: [kubernetes.dashboards.$DOMAIN]
  tls:
  - hosts:
    - kubernetes.dashboards.$DOMAIN
  annotations:
    nginx.ingress.kubernetes.io/auth-signin: https://auth.$DOMAIN.com/oauth2/start
    nginx.ingress.kubernetes.io/auth-url: https://auth.$DOMAIN.com/oauth2/auth
